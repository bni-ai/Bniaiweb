## 1. Marketing route shell

- [x] [P] Implement public landing page routing per **d1 — route and layout** in design.md so `/` renders a marketing-only shell with header, footer, and a `/login` entry point; verify by loading the public-landing-page route and confirming no member or admin navigation is present.

## 2. Marketing content sections

- [x] [P] Implement the hero and CTA sections per **d2 — page sections** and **d3 — visual design** in design.md so the page introduces BNI 華AI分會, scrolls to features, and exposes the `mailto:` join action; verify by exercising the public-landing-page section-order and action scenarios at `/`.
- [x] [P] Implement the stats and features sections per **d2 — page sections** and **d3 — visual design** in design.md so the public-landing-page content presents chapter proof points and member benefits with responsive cards; verify by exercising the public-landing-page section-order and mobile-layout scenarios on narrow and wide viewports.
